# @w3booster/datamodel

Shared TypeScript models, Parse services, and model services used by the client, master server, and overlay apps.

This package replaces the former `src/app/data/common` Git submodule. The apps keep that path as a symlink so existing imports continue to work during the migration.
