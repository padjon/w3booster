# @w3booster/w3blib

Native C++ Warcraft III integration code, including the DLL injector and Visual Studio solution.

The automated pnpm build currently documents the manual build entry point instead of invoking MSBuild because this package depends on a Windows native toolchain.
